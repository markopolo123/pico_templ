name: CI

on:
  push:
    branches:
      - 'feat/**'
      - 'feature/**'
      - 'fix/**'
      - 'bugfix/**'
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Flox
        uses: flox/install-flox-action@bdb1ae4896548269e6ee2019dd8bf48f87477a34 # v3

      - name: Run pre-commit
        run: flox activate -- prek run --all-files

      - name: Run tests
        run: flox activate -- just test
